# Codex subscription writer/reviewer; OpenRouter is only the review fallback.
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
WRITER_PROVIDER=codex
WRITER_MODEL=gpt-5.6-sol
WRITER_TIMEOUT_SEC=600
# Codex CLI >=0.153.1, authenticated using ChatGPT as the service user.
# Linux deployment: /opt/research-agent-codex/node_modules/.bin/codex
# CODEX_WRITER_BIN=/absolute/path/to/codex
# Explicit rollback only: WRITER_PROVIDER=openrouter, WRITER_MODEL=z-ai/glm-5.3-flash
JUDGE_MODEL=gpt-5.6-terra
FALLBACK_MODEL=z-ai/glm-5.3-flash

# Final-layer reviewer controls
FINAL_LAYER_REVIEWER_MODEL=gpt-5.6-terra
FINAL_LAYER_FALLBACK_MODEL=z-ai/glm-5.3-flash
FINAL_LAYER_LOW_PATCH_FALLBACK_MODEL=
# Codex reviewer always makes one bounded attempt before technical failover.
FINAL_LAYER_PRIMARY_ATTEMPTS=1
FINAL_LAYER_FALLBACK_ATTEMPTS=1
FINAL_LAYER_REVIEW_TIMEOUT_SEC=120
FINAL_LAYER_MAX_COST_USD=1.0

# Researka submission (optional — leave blank to skip submission)
RESEARKA_URL=
RESEARKA_V2_API_KEY=
AGENT_ID=research-agent-bot

# Safety controls
BOT_ENABLED=true
BOT_SUBMIT_ENABLED=true
DAILY_COST_CAP_USD=10.0

# === Database source keys (OPTIONAL — improves rate limits) ===

# NCBI E-utilities API key — bumps PubMed rate limit 3/s → 10/s.
# Free; register at https://account.ncbi.nlm.nih.gov/settings/
NCBI_API_KEY=

# Semantic Scholar API key — higher rate limit on graph API.
# Free tier (no key) is ~100 req/5min — usually sufficient.
# Register at https://www.semanticscholar.org/product/api/tutorial
SEMANTIC_SCHOLAR_API_KEY=

# CORE — open-access aggregator (200M+ records). Free tier
# requires registration: https://core.ac.uk/services/api
CORE_API_KEY=

# Polite-pool emails (optional, no registration). Crossref +
# Unpaywall reward identification with faster responses.
CROSSREF_POLITE_EMAIL=
UNPAYWALL_EMAIL=
