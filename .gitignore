.venv/
__pycache__/
.pytest_cache/
*.pyc
# Ignore runs/ contents EXCEPT e2e-baselines (v4 Rule 26 metric-loop artifacts)
# AND the small set of "proof receipts" PROJECT_STATE references as evidence
# of the trust-spine claims (Day 8.2 reviewer P2: a fresh clone must include
# the audit artifacts that back the status table; otherwise the docs cite
# evidence the cloner can't see).
# Note: must use runs/* not runs/ so the negation pattern can re-include
# subdirectories — git won't recurse into a wholly-ignored dir.
runs/*
!runs/e2e-baselines/
runs/e2e-baselines/*.tmp
# Proof receipts referenced in PROJECT_STATE — tracked as audit evidence.
# Pre-Day-9.1 receipts (baseline run-rates that motivated the AAA push):
!runs/metformin-001-2026-04-28T17-42-13Z-29dd/
!runs/rapamycin-002-2026-04-28T17-43-13Z-89e6/
!runs/rapamycin-002-2026-04-28T17-37-47Z-ecd5/
!runs/everolimus-003-2026-04-28T17-44-06Z-15a3/
!runs/everolimus-003-2026-04-28T17-22-15Z-288e/
# Post-Day-9.1 receipts (numeric_in_text trace closes auditor's main
# rejection; rapamycin run-rate jumped from 1/4 to 4/5 accept):
!runs/metformin-001-2026-04-28T18-26-46Z-c1e0/
!runs/rapamycin-002-2026-04-28T18-23-28Z-2f4e/
!runs/everolimus-003-2026-04-28T18-28-43Z-0088/
# Day 9.2 best-of-5 canonical receipts (each carries best_of_n_manifest.json
# listing all 5 attempts; --best-of 5 produces deterministic accept_* on
# all three drugs):
!runs/metformin-001-2026-04-28T18-38-21Z-fdcc/
!runs/rapamycin-002-2026-04-28T18-41-35Z-dc46/
!runs/everolimus-003-2026-04-28T18-47-44Z-734c/
# Day 9.3 live-retrieval canonical-anchoring receipt — first --live run
# where ALL 4 metformin canonical trials (MASTERS / MET-PREVENT / TAME /
# MILES) surface in the corpus + accept_clean on a single attempt:
!runs/metformin-001-2026-04-28T19-01-44Z-d1f4/
# Day 10.6 — first synthesis paper: 12 metformin claim receipts → one
# integrated paper_synthesis.md, audit score 10/10 across 7 quality
# checks (all 3 load-bearing Q1/Q3/Q5 pass). Caveat: false-positive,
# all 12 receipts anchored on MASTERS NCT02308228 (duplicate aggregation,
# not real cross-source — closed by Day 10.7+10.8a gate fixes).
!runs/synthesis-metformin-010-2026-04-29T05-52-53Z-caa9/
# Day 10.8c — first real cross-source proof:
#   producer: 13 cluster receipts from a single metformin --multi-receipt run
!runs/metformin-multi-001-2026-04-29T09-23-01Z-44b9/
#   consumer: synthesis across 13 unique source units (7 NCTs + 6 untrialed),
#   audit 8.3/10 (Q3 load-bearing fail on direct/indirect transition — system
#   correctly ship-blocks). Architecture verified end-to-end; gap was
#   writer-side, closed in Day 10.9 below.
!runs/synthesis-metformin-010-2026-04-29T09-32-28Z-0d63/
# Day 10.9 — initially claimed AAA at 10.0/10 (REVERTED by Day 10.10
# as gamed: pair-coverage validator loosening + cosmetic transition
# auto-prefix + SPAR-rejected receipts cited as evidence). Kept here
# as audit evidence of what got reverted.
!runs/synthesis-metformin-010-2026-04-29T09-52-15Z-044b/
# Day 10.10 — TRACE_BACKEND=http multi-receipt re-run on metformin
# corpus: 30 clusters produced, only 1 accepted by SPAR (cluster_01).
# Honest result: trust-spine ordering correctly blocks synthesis on a
# corpus where SPAR rejected 29 of 30 clusters. Live trace did NOT
# materially raise the accept rate vs fixture mode — the rejections
# are real claim-quality issues (protocol-as-claim, mechanism
# inflation, sample-size critiques), not trace coverage gaps.
!runs/metformin-multi-001-2026-04-29T10-27-39Z-4923/
# Day 10.11 — TRACE_BACKEND=http re-run AFTER protocol-as-claim filter
# shipped: 27 clusters, 1 accepted (1 accept_clean + 26 rejected).
# The Day 10.11 fact_extractor filter (`check_objective_as_claim`)
# successfully dropped objective-span extractions upstream — visible
# in the cluster count dropping from 30 to 27 vs Day 10.10. But the
# SPAR pass rate stayed ~4% because the dominant rejection mode
# shifted to mechanism inflation / sample-size critiques / missing
# source text. AAA still not reached on this corpus; the filter is
# correct but insufficient for the empirical bar. Day 10.12+ needs
# to address the next dominant failure mode.
!runs/metformin-multi-001-2026-04-29T11-00-43Z-50c9/
# Day 10.12 — TRACE_BACKEND=http re-run AFTER mechanism-inflation guard
# + protocol-only-cluster filter shipped:
#   22 clusters (vs 27 pre-fix); 2 accept_caveated, 20 rejected;
#   7 protocol-only clusters excluded by the new filter (logged in
#   manifest.protocol_only_excluded_indices). Real progress vs Day 10.11
#   (accept rate 4% → 9%) but still below the cross-source ≥3 floor.
# Honest interpretation: the filter+guard slice of work has plateaued
# on this corpus — remaining accept-rate gap is corpus-quality bound.
!runs/metformin-multi-001-2026-04-29T11-50-04Z-de47/
tests_legacy/golden/fixture_runs/
tests_legacy/golden/fixture_runs_debug/
*.egg-info/
.env*
!.env.example

scripts/karpathy-loop/snapshots/
scripts/karpathy-loop/diffs/

.DS_Store
# macOS / iCloud auto-versioning leaves "<name> 2.<ext>" duplicates next to
# files when they're rewritten. They've snuck into commits before — block them.
* 2.*
* [0-9].*

scripts/karpathy-loop/snapshots/
scripts/karpathy-loop/diffs/

docs/weekly/
